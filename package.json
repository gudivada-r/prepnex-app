{
    "name": "project-root",
    "private": true,
    "engines": {
        "node": ">=20.0.0"
    },
    "workspaces": [
        "frontend"
    ],
    "scripts": {
        "build": "cd frontend && npm install && npm run build"
    },
    "devDependencies": {
        "vite": "^7.2.4"
    },
    "overrides": {
        "react": "^19.2.0",
        "react-dom": "^19.2.0"
    }
}